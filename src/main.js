import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // seu CSS (Tailwind ou outro)

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
