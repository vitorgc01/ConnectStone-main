import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";  // Aqui está o Tailwind carregando o CSS

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
